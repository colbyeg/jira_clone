import React from 'react';

import { StyledButton } from './Styles';

// const ListAdd = () => <StyledButton>Add List</StyledButton>;

const ListAdd = () => <StyledButton>Add List</StyledButton>;

export default ListAdd;
